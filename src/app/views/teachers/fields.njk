<div class="item">
    <div>Avatar URL</div>
    <div>
        <input 
            type="url" 
            name="avatar_url" 
            placeholder="http://" 
            value="{{ teacher.avatar_url }}">
    </div>
</div>

<div class="item">
    <div>Nome completo</div>
    <div>
        <input 
            type="text" 
            name="name" 
            placeholder="Nome do Professor" 
            value="{{ teacher.name }}">
    </div>
</div>

<div class="item">
    <div>Data de nascimento</div>
    <div>
        <input 
            type="date" 
            name="birth" 
            value="{{ teacher.birth }}">
    </div>
</div>

<div class="item">
    <div>Grau de escolaridade</div>
    <div>
        <select name="schooling">
            <option value="EM"
            {% if teacher.schooling == "EM"%}selected{%endif%}}
            >Ensino Médio Completo</option>

             <option value="ES"
            {% if teacher.schooling == "ES"%}selected{%endif%}}
            >Ensino Superior Completo</option>

             <option value="M"
            {% if teacher.schooling == "M"%}selected{%endif%}}
            >Mestrado</option>

             <option value="D"
            {% if teacher.schooling == "D"%}selected{%endif%}}
            >Doutorado</option>
        </select>
    </div>
</div>

<div class="item">
    <div>Tipo de aula</div>
    <div>
        <span>
            <input 
                type="radio" 
                name="classes" 
                value="P" 
                {% if teacher.gender == 'P'%} 
                    checked=
                {% endif %}> Presencial
        </span>
        <span>
            <input 
                type="radio" 
                name="classes" 
                value="D"
                {% if teacher.gender == 'D'%} 
                    checked=    
                {% endif %}> Á distância
        </span>
    </div>
</div>

<div class="item">
    <div>Acompanhamento</div>
    <div>
        <input 
            type="text" 
            name="services" 
            placeholder="Separe por vírgula. Ex.: Matemática, Física"
            value="{{ teacher.services }}"
        >
    </div>
</div>
<button type="submit">Salvar</button>